<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title%></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body >
    <header class="bg-colour py-4 border-bottom shadow">
        <div class="container d-flex justify-content-between align-items-center">
                <div class="logo">
                    <h2 class="m-0">BloggeR</h2>
                </div>
                <nav class="nav_bar align-items-center d-flex ">
                    <ul class=" gap-5 m-0 d-flex  list-unstyled fw-bolder pe-5">
                        <li><a href="#"></a>Home</li>
                        <li><a href="#"></a>Contact</li>
                        <li><a href="#"></a>About</li>
                    </ul>
                </nav>
                
                <% if (user) { %>
                    <div class="d-flex  flex-row gap-3">
                        <a href="/logout" class="btn  fw-bolder p-1 btn-outline-dark border-2">Log out</a>
                        <a href="/profile/<%= user._id %>" class="btn  fw-bolder p-1 btn-outline-dark px-2  border-2 fs-6"><i class="fa-regular fa-user user_profile"> <%= user.username %></i></a>
                    </div>
                <% } else { %>  
                <div>
                    <a href="/signIn" class="btn me-5 fw-bolder p-3 btn-outline-dark border-2">Sign In</a>
                </div>
                <% } %>
        </div>
    </header>
    <main class="container d-flex flex-column px-4 py-5 no-scrollbar main-container overflow-hidden position-relative">
        <div class="post-container overflow-hidden overflow-y-scroll d-flex flex-column gap-3 h-100">
            <% if (user) { %>
                <div class="d-flex py-4">
                    <h2 class="flex-grow-1">Latest Blogs</h2>
                    <a href="/addPost" class="btn  fw-bolder p-1 btn-outline-dark border-2 d-flex align-items-center gap-1"> <i class="fa-solid fa-plus pt-1"></i> create your Blog</a>
                </div>

            <% } %>
            <% posts.forEach(element => { %>
                <div class="posts border p-2 bg-gradient">
                    <a href="/viewPost/<%= element._id%>" class="fs-2 text-decoration-none text-dark mb-4"><%= element.title %></a>
                    <p class="fw-semibold"><i class="fa-regular fa-user fw-medium"></i>  <%= element.author.username %></p>
                    <p class="fw-normal post-content"><%= element.content %></p>
                </div>
            <% }) %>
        </div>
    </main>
    <%- include('../footers/footers.ejs') %>
    
